# sample-app

Demo/integration-test app for `droidshield-sdk`. Currently an empty shell
(no UI, no checks wired) — will exercise the `DroidShield` facade once
`droidshield-sdk` has one (DECISIONS.md D023).
